# Дельта от 17.09.2026 (вторая) — вёрстка блога

Заливать в репозиторий: Add file → Upload files → перетащить папку `app` и файл `CHANGELOG.md` → Commit.

**Важно:** сначала должна быть залита первая дельта (`btctour-blog-delta-2026-09-17.zip`) — эта продолжает её.

## Что изменилось

| Файл | Что |
|---|---|
| `app/blog/page.tsx` | «Свежие статьи» и блок «Нужен совет по датам» подняты к заголовку |
| `app/globals.css` | новое оформление таблиц в статьях |
| `app/blog/[slug]/page.tsx` | таблицы обёрнуты в блок с горизонтальной прокруткой |
| `CHANGELOG.md` | пункт 53 |

## Список статей

Заголовок «Блог о Байкале» и подпись под ним переехали внутрь левой колонки — так же, как на странице статьи. Боковая колонка теперь начинается вровень с заголовком, а не под ним.

Страницы пагинации (`/blog/stranitsa/2/` и дальше) не менялись: боковой колонки там нет.

## Таблицы

Своих стилей у таблиц не было, поэтому работали браузерные: заголовки по центру, колонки разной ширины, без линий и отступов.

Стало: шапка на светлой подложке, заголовки по левому краю, строки разделены линией, чётные слегка подкрашены, первая колонка полужирная. На телефоне таблица прокручивается вбок во всю ширину экрана, а не сжимается в нечитаемые столбики.

## Что проверить после заливки

1. `/blog/` на компьютере — «Свежие статьи» вровень с заголовком.
2. `/blog/kogda-ehat-na-baykal-zimoy/` — таблицы «Коротко: когда что» и «Какой тур брать».
3. Та же страница на телефоне — таблица прокручивается пальцем, страница целиком вбок не едет.

Если оформление таблиц покажется слишком строгим или наоборот пёстрым, скажите — уберу подкраску строк или сделаю линии светлее.
